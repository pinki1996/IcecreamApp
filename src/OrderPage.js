import React from 'react'

const OrderPage = () => {
  return (
    // {Math.ceil(Math.random() * 100)}
    <div>
      <h2>Your Order has been placed successfully and your order ID is:<h1 data-testid="id">400</h1> </h2>
    </div>
  )
}

export default OrderPage